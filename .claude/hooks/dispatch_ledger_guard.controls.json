{
  "positive": [
    {
      "name": "dispatch-mode flag pinned live for session c383d83c-... (2026-09-13, observation 0062) + a cwd that genuinely has no .dispatch/runs ledger beneath it \u2014 guard fails closed. Session id is the literal one captured 2026-09-11; pin_dispatch_flag stamps it into a throwaway flag file with a fresh timestamp at check time, so this no longer depends on the real ~/.claude/dispatch-mode.json naming this same session, which stopped being true the first time any other real session armed that file for itself.",
      "argv": [],
      "stdin": "{\"tool_name\": \"Task\", \"session_id\": \"c383d83c-5af0-4a61-98ee-c4b6376d6fca\", \"cwd\": \"./.no-ledger-here\"}",
      "pin_dispatch_flag": true,
      "expect_exit_in": [
        2
      ],
      "expect_contains": "no current run ledger was found"
    },
    {
      "name": "fixtures/window-fit/ledger.md is the REAL ledger of dispatch run 2026-09-17-dispatchwright-window-fit copied verbatim (one open row, U2, estimated_tokens 400000, surface x2, pre-1.3.0 columns); the sibling `session` file names the same literal session id the other controls use, so ledger_is_current() holds on the session-name path regardless of the fixture's mtime; pin_dispatch_flag stamps a throwaway flag fresh at check time. Handed on argv (--ledger) because .dispatch/ is gitignored in this repo; the path is relative and resolves against the hook's own directory (fixtures installed beside the hooks). Windows fixture: the documented statusline shape with five_hour at 97.5% (fixtures/window-fit/usage-windows-five-hour-97.json, written by usage_windows.py). The guard fails closed and names the reset time; the row has no `next` window cell and the call is not a resume, so no exemption.",
      "argv": [
        "--ledger",
        "fixtures/window-fit/ledger.md",
        "--windows-file",
        "fixtures/window-fit/usage-windows-five-hour-97.json"
      ],
      "stdin": "{\"tool_name\": \"Task\", \"session_id\": \"c383d83c-5af0-4a61-98ee-c4b6376d6fca\", \"cwd\": \"./.no-ledger-here\", \"tool_input\": {\"description\": \"verify findings\", \"prompt\": \"check the findings\"}}",
      "pin_dispatch_flag": true,
      "expect_exit_in": [
        2
      ],
      "expect_contains": "97.5% used"
    },
    {
      "name": "fixtures/window-fit/ledger.md is the REAL ledger of dispatch run 2026-09-17-dispatchwright-window-fit copied verbatim (one open row, U2, estimated_tokens 400000, surface x2, pre-1.3.0 columns); the sibling `session` file names the same literal session id the other controls use, so ledger_is_current() holds on the session-name path regardless of the fixture's mtime; pin_dispatch_flag stamps a throwaway flag fresh at check time. Handed on argv (--ledger) because .dispatch/ is gitignored in this repo; the path is relative and resolves against the hook's own directory (fixtures installed beside the hooks). Windows fixture: seven_day at 99% (usage-windows-seven-day-99.json) with the doctrine's calibration SHAPE (usage-calibration-shape.json: 7d 250000 tokens/%, 2 samples -- the worked example, not this rig's measurement). Remaining 7d allowance = 1% x 250000 x 0.85 = 212,500 < the 400,000 the open row estimates, so the guard prints the WARNING line with the numbers into additionalContext and still allows (exit 0).",
      "argv": [
        "--ledger",
        "fixtures/window-fit/ledger.md",
        "--windows-file",
        "fixtures/window-fit/usage-windows-seven-day-99.json",
        "--calibration-file",
        "fixtures/window-fit/usage-calibration-shape.json"
      ],
      "stdin": "{\"tool_name\": \"Task\", \"session_id\": \"c383d83c-5af0-4a61-98ee-c4b6376d6fca\", \"cwd\": \"./.no-ledger-here\", \"tool_input\": {\"description\": \"verify findings\", \"prompt\": \"check the findings\"}}",
      "pin_dispatch_flag": true,
      "expect_exit_in": [
        0
      ],
      "expect_contains": "WARNING"
    }
  ],
  "negative": [
    {
      "name": "dispatch-mode flag pinned live for session c383d83c-... (2026-09-13, observation 0062) + the frozen, session-tied ledger fixture at workshop/estate/observer/fixtures/dispatch-ledger-current -- ledger is tiered and inside the wave cap, guard allows. The ledger body is a REAL ledger copied verbatim off the rig (2026-09-10-estate-audit); the sibling `session` marker beside it is what ledger_names_session() reads, so ledger_is_current() holds on the session-name path and NOT on mtime. pin_dispatch_flag replaces this control's second dependency on real machine state (the live ~/.claude/dispatch-mode.json correlating to this same session) with a throwaway flag file stamped fresh at check time, using the same literal session id captured 2026-09-11 -- so this case now proves the ledger-allows path deterministically instead of by coincidence with whatever session last armed the real flag. The `session` file and this session_id are one fact written twice: if they drift, this control silently falls back to the mtime-freshness path and starts rotting again.",
      "argv": [],
      "stdin": "{\"tool_name\": \"Task\", \"session_id\": \"c383d83c-5af0-4a61-98ee-c4b6376d6fca\", \"cwd\": \"estate/observer/fixtures/dispatch-ledger-current\"}",
      "pin_dispatch_flag": true,
      "expect_exit_in": [
        0
      ]
    },
    {
      "name": "fixtures/window-fit/ledger.md is the REAL ledger of dispatch run 2026-09-17-dispatchwright-window-fit copied verbatim (one open row, U2, estimated_tokens 400000, surface x2, pre-1.3.0 columns); the sibling `session` file names the same literal session id the other controls use, so ledger_is_current() holds on the session-name path regardless of the fixture's mtime; pin_dispatch_flag stamps a throwaway flag fresh at check time. Handed on argv (--ledger) because .dispatch/ is gitignored in this repo; the path is relative and resolves against the hook's own directory (fixtures installed beside the hooks). Windows fixture: the documented reading as-is (5h 23.5%, 7d 41.2%; usage-windows-documented.json) with the same calibration shape. Both windows leave far more than 400,000 tokens, so the guard allows with no warning and no block -- and, in fixture mode, writes nothing into the ledger.",
      "argv": [
        "--ledger",
        "fixtures/window-fit/ledger.md",
        "--windows-file",
        "fixtures/window-fit/usage-windows-documented.json",
        "--calibration-file",
        "fixtures/window-fit/usage-calibration-shape.json"
      ],
      "stdin": "{\"tool_name\": \"Task\", \"session_id\": \"c383d83c-5af0-4a61-98ee-c4b6376d6fca\", \"cwd\": \"./.no-ledger-here\", \"tool_input\": {\"description\": \"verify findings\", \"prompt\": \"check the findings\"}}",
      "pin_dispatch_flag": true,
      "expect_exit_in": [
        0
      ],
      "expect_not_contains": "WARNING"
    }
  ]
}
