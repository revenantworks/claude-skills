# Trigger Evals — description tuning

Provenance: derived from revenantworks-foundation-skillwright v1.0.0, 2026-07-14 (queries 27–30 added for Entry — Integrate; 31–34 for Entry — Pack; refreshed 2026-07-23 for the 1.1.0 decoupling — #7 now covers Entry — Upkeep, #8 flipped to the brandwright boundary). Re-anchored to v1.2.0, 2026-07-24: item ⑤ added a release-only reference file and changed no description text, so no query was rewritten and no re-run was owed — the 34 queries stand as re-judged cold against the slimmed descriptions earlier the same day (RESULTS.md). Re-anchored to v1.2.2, 2026-07-24: the assertion-suite repair pass edited body doctrine and four asserts, not the `description`, so routing is untouched — no query rewritten, no re-run owed, and the 34-query pass rate above is not restated as a fresh result. Re-anchored to v1.2.3, 2026-07-24: same standing — the correction to Build step 6's enforcement claim touched body doctrine and one assert, never the `description`; no query rewritten, no re-run owed. Re-anchored to v1.2.4, 2026-07-25: the bare-invocation reply and the Restraint cap changed inside the body; the `description` — which already enumerated all five subcommands, `port` included — is byte-identical, so routing is untouched and query #3 ("skillwright") still fires on the same text. No query rewritten, no re-run owed. **Re-anchored to v1.3.0, 2026-07-25, and this one DOES owe a re-run:** the `description` was rewritten to close the skillwright ↔ commwright seam — a prose-pass clause added (791 → 772 chars), the `keep going` gloss and the `.skill, zip, or Claude Code plugin` packaging tail traded for it. Queries 35–37 are new and cover the added claim and its message-side boundary; no existing query was rewritten. **Nothing below is a fresh result:** the three new rows are authored-not-executed, and rows 8, 9, 18 and 30 all sit on text this edit touched, so the whole suite is owed a cold re-run against the amended listing before 37/37 is claimed anywhere. Re-anchored to v1.3.1, 2026-07-25: a record-only patch that put the pack router's then-unamended skillwright row on the books; the `description` is byte-identical to 1.3.0's, so no query was rewritten, no row was added, and the re-run owed above is neither discharged nor enlarged. Note the blind spot it names — **every query here judges the eight `description` fields cold and none reads `packs/foundation/CLAUDE.md`**, so whatever that always-on surface does with a prose ask is outside anything a pass rate here can report. **Re-anchored to v1.3.2, 2026-07-25:** the router row was amended the same day and the seam is now closed on that surface too; the `description` is again byte-identical, so no query was rewritten, no row was added, and the re-run owed above still stands. The blind spot is unchanged by the close — no query here reads that file either way. **Re-anchored to v1.4.0, 2026-07-27, and this one DOES owe a re-run:** `Entry — Audit` gained the security pass and the `description` was rewritten around it (772 → 798 chars) — a security clause added, a reciprocal agentwright boundary added, and five existing fragments traded for them (below). Queries 38–43 are new: three cover the added security claim, three test the agentwright boundary and the code-level line beyond it. **Nothing below is a fresh result:** the six new rows are authored-not-executed, and rows 4, 5, 8, 14, 21–23, 25, 26, 28, 31, 32 and 34 all sit on text this edit touched, so the whole suite is owed a cold re-run against the amended listing before 43/43 is claimed anywhere. **DISCHARGED 2026-07-27** (`evals/RESULTS.md`): all 43 judged cold by two independent judges holding only the listing and their own query slice — **40 clean, 0 FAIL, 3 AMBIGUOUS (#8, #26, #35)**. 43/43 was never available and is not claimed. **#26 broke exactly as predicted above** — the judge named the removed `Every build ships trigger evals.` string verbatim without knowing it had been traded. #35 reopens the skillwright <-> commwright seam on commwright's unscoped `humanize` verb, and #8 exposes a circular pair of reciprocal boundary sentences with brandwright. **All six security rows resolved cleanly**, #42 against #38 included. The 37/37 from 2026-07-25 stands as the last executed *assertion* result and is not restated as current. **Re-anchored to v1.0.0 (wright re-baseline), 2026-07-31:** the member was renamed to the wright motif and its version designation reset to 1.0.0; suite content carried forward unchanged, no case, input, assert, or count moved. Earlier version numbers in this line are predecessor-era designations. **Re-anchored to v1.1.0, 2026-08-07:** the additions are reference doctrine plus two body clauses; the `description` is byte-identical to 1.0.6's, so no query was rewritten, no row was added, and no re-run is owed. The re-run standing recorded above is neither discharged nor enlarged. **Re-anchored to v1.1.1, 2026-08-08:** row 31's query role swapped — "a presales engineer" → "a customer-support engineer" (2026-08-08 estate audit, owner judgment: firewall distance) — same Entry — Pack shape, same Expected, the `description` untouched, no other row moved; still 43. Row 31's cold re-judge against the swapped wording is recorded in `evals/RESULTS.md` (2026-08-08). The re-run standing recorded above is neither discharged nor enlarged. **Re-anchored to v1.2.0, 2026-08-12, and this one DOES owe a re-run:** the `description`'s closing boundary gained two clauses (2026-08-12 estate audit, finding 16) — a token/cost-cut-on-a-conforming-SKILL.md handoff to tokenwright and an eval-suite-as-its-own-job handoff to evalwright (798 → 939 chars) — closing the two one-sided pairs both neighbours already push from their side. No existing query was rewritten and no row was added, so the count stands at 43; rows whose queries sit on 'slim this SKILL.md' or 'write trigger evals' shapes are the ones the re-judge should hit first. Body changes in the same release (Build step 3 and Refresh injection rule, Packaging step 4 re-homed to Rubric A, the shell/python3 dependency declaration, the rubrics.md TOC-threshold variance note) touch no routing text. **Re-anchored to v1.2.1, 2026-08-14:** a body-only change (the Upkeep-entry injection rule, 2026-08-14 estate audit) plus a pointer clause in `upkeep-doctrine.md`; the `description` is byte-identical to 1.2.0's, so the routing surface these 43 queries judge did not move — no query, expectation, or count touched; still **43**, and the re-run 1.2.0 owes remains owed. **Re-anchored to v1.3.0, 2026-08-15:** all changes are reference doctrine (Rubric A dimension 11, two registry seam rows, a registry Contents block, the upkeep-doctrine hop close); the `description` is byte-identical to 1.2.1's, so the routing surface these 43 queries judge did not move — no query, expectation, or count touched; still **43**, and the re-run 1.2.0 owes remains owed. The seam rows the registry gained are the declared form of boundaries rows 35–37 and the tokenwright/evalwright handoff clauses already judge from the description side. **Re-anchored to v1.3.1, 2026-08-17:** body and reference changes only (2026-08-17 estate audit + security scan + Rubric A refresh — the data-never-instructions rule on Build step 1 and Integrate step 1, the hidden-text clause in the security-pass paragraph, the restamped baseline with its frontmatter-shape bullet); the `description` is byte-identical to 1.3.0's, so the routing surface these 43 queries judge did not move — no query, expectation, or count touched; still **43**, and the re-run 1.2.0 owes remains owed. Assertion cases 41–45 were added in `test-cases.md` the same day; none is a trigger row. **Re-anchored to v1.3.3, 2026-08-20 — provenance only, nothing executed here:** the pack-wide audit corrected the false 'at most two' Load-budget sentence and added the invocation-control line to Behavior notes. The `description` is byte-identical, so the routing surface these queries judge did not move; no query, expectation, or count touched. The 43-query cold re-run owed since v1.2.0 remains owed, neither discharged nor enlarged. **Re-anchored to v1.3.4, 2026-09-09 — provenance only, nothing executed here:** `references/pack-registry.md` budget-row updates only, `description` untouched; no query, expectation, or count moved. **Re-anchored to v1.3.5, 2026-09-10 — provenance only, nothing executed here:** the Load budget statement correction, the standalone-profile amendment in `rubrics.md`, and the tokenwright freeze note in `pack-registry.md` are all doctrine and registry prose; `description` untouched; no query, expectation, or count moved. **Re-anchored to v1.3.6, 2026-09-10 — provenance only, nothing executed here:** Cases 41-45 heading corrections (estate finding `eval-ledgers-underreport-executed-probes`); not a routing-surface row, `description` untouched, no query, expectation, or count moved. **Re-anchored to v1.3.7, 2026-09-10 — provenance only, nothing executed here:** `references/pack-registry.md` budget-row update for dispatchwright's declared body budget; registry prose, not this member's own body, `description` untouched, no query, expectation, or count moved. **Re-anchored to v1.3.8, 2026-09-11 — provenance only, nothing executed here:** `references/pack-registry.md` roster update for the new member resumewright (roster row, budget row, the dispatchwright ↔ resumewright seam row); registry prose, not this member's own body, `description` untouched, no query, expectation, or count moved. **Re-anchored to v1.3.9, 2026-09-11 — provenance only, nothing executed here:** the same Packaging, Entry — Audit and reference-doctrine additions recorded in `test-cases.md`'s 1.3.9 anchor (task-observer observations #0011, #0021, #0025, #0031, #0034); `description` untouched, no query, expectation, or count moved. **Re-anchored to v1.3.10, 2026-09-12 — provenance only, nothing executed here.** In full: the 1.3.10 bump is registry data only — `references/pack-registry.md` gained the godotsmith roster row, its budget row and the gamedev pack's first routing-seam table. No doctrine, entry point or rule changed and the `description` field is byte-identical, so the routing surface these queries judge did not move and no re-judge is owed. **Re-anchored to v1.4.0, 2026-09-13 — provenance only, nothing executed here** (recorded retroactively; owed the same day and missed): Build step 4 (per-partition niche verdict) and Restraint's fourth condition are body-only; the `description` is byte-identical, so the routing surface these queries judge did not move; no query, expectation, or count touched. **Re-anchored to v1.4.1, 2026-09-13 — provenance only, nothing executed here:** `references/pack-registry.md` budget-row update for rigwright's own 1.1.9 (task-observer observation #0062); registry prose, not this member's own body, `description` untouched, no query, expectation, or count moved. **Re-anchored to v1.4.3, 2026-09-14 — provenance only, nothing executed here** (recording the v1.4.2 anchor that bump missed): 1.4.2's upkeep mapping row (#0073), and 1.4.3's re-anchor plus a registry budget row for resumewright's 1.1.0, are body and registry prose; `description` untouched, no query, expectation, or count moved. **Re-anchored to v1.4.4, 2026-09-14 — provenance only, nothing executed here:** the release doctrine's green-gate rule (#0077) and dispatchwright's registry budget row are reference and registry prose; `description` untouched, no query, expectation, or count moved.

**What the description traded (v1.4.0), and the rows that ride on it.** Removed to pay for the two additions inside the pack's 600–800 band: `Every build ships trigger evals.` (−32, the row at risk is **#26** — evalwright's own description still defers builds by name, which is what should carry it now) · `Builds spec-clean neutral.` (−27, **#8/#17** rest on the brandwright boundary sentence per the 2026-07-25 run, not on this claim) · `(roster, registry, release set)` (−32, **#28** keeps the propagate clause head) · `subcommands` and the `— staleness sweep` gloss (−30, all five keywords retained, **#3/#7/#10/#27/#32** unaffected) · `create,` / `improve,` from the verb list and `and build` from the pack clause (−27, **#1/#2/#31** ride `build`) · `docs` from the prose-pass file list (−6, README and CLAUDE.md — the two **#35/#36** actually use — retained). `pack-integrates` → `integrates` and the opening recast dropped 8 more. Every traded fragment is named here because each was retained-verbatim at 1.3.0 for a row that passes on it; **#26 is the one to judge first on the re-run.**

Forty-three queries for checking whether the `description` fires skillwright at the right times. Twenty-two should trigger, twenty-one should not. Manual checklist, fully self-contained — read each cold against name + description only and compare to the expected column.

| # | Query | Should trigger? | Why |
|---|---|---|---|
| 1 | "Build me a skill that summarizes legal contracts into risk memos." | ✅ yes | Build-from-scratch, the core case |
| 2 | "Turn the workflow we just did into a skill." | ✅ yes | Extract-from-conversation build |
| 3 | "skillwright" | ✅ yes | Bare invocation |
| 4 | "Audit my skill against current best practices." | ✅ yes | Audit-existing |
| 5 | "Does this SKILL.md meet the spec? Score it." | ✅ yes | Audit + scoring |
| 6 | "Is there actually a niche for a skill that does invoice triage?" | ✅ yes | Niche/market analysis |
| 7 | "skillwright upkeep" | ✅ yes | Pack-wide staleness sweep |
| 8 | "Apply my company's branding to the skills we generate." | ❌ no | Brand application — brandwright's job, on invoke |
| 9 | "Package this skill folder so I can install it." | ✅ yes | Release packaging |
| 10 | "skillwright refresh" | ✅ yes | Baseline maintenance |
| 11 | "Write me a prompt that summarizes legal contracts." | ❌ no | Prompt, not skill — promptwright's job |
| 12 | "Improve this system prompt for my support agent." | ❌ no | Prompt improvement — promptwright |
| 13 | "What are Claude skills and how do they work?" | ❌ no | Explanation, no artifact to build |
| 14 | "Install the PDF skill for me." | ❌ no | Installation, not authoring |
| 15 | "Build me a web app that tracks invoices." | ❌ no | Software build, not a skill package |
| 16 | "Summarize this document." | ❌ no | Wants the task done, not a skill built |
| 17 | "Design a logo and brand palette for my company." | ❌ no | Brand creation, not brand-to-skill configuration |
| 18 | "Write a README for my Python library." | ❌ no | Docs for code, not a skill package |
| 19 | "Which model should I run this prompt on?" | ❌ no | Model routing — promptwright |
| 20 | "Make me a project plan for Q3." | ❌ no | Planning task, unrelated |
| 21 | "Port this skill pack so I can use it at my job — branding out, new names." | ✅ yes | Port — rebrand for a new owner |
| 22 | "Strip the branding and any personal references out of these skills." | ✅ yes | Port — sanitize sweep |
| 23 | "Make this skill set neutral and rename everything for a client handoff." | ✅ yes | Port — neutral re-issue |
| 24 | "Port this python app to linux." | ❌ no | Software porting — no skill set involved |
| 25 | "Write the trigger evals and test cases for my existing skill — don't touch the skill itself." | ❌ no | Suite authoring for an existing target — evalwright |
| 26 | "Build me a skill that triages invoices, and make sure it ships with a full eval suite." | ✅ yes | The build owns the package; the suite ships via the evalwright handoff |
| 27 | "skillwright integrate tokenwright" | ✅ yes | Direct integrate invocation |
| 28 | "Add the new member to the pack and update the rest of the skills with the integration." | ✅ yes | Pack propagation in natural language — roster restamp + registry + release set |
| 29 | "Integrate my app with the Stripe API." | ❌ no | Software integration, not pack propagation — no skill, pack, or roster in sight |
| 30 | "keep going" | ❌ no | Bare continuation outside a pack build's offer — ordinary conversation; only the continuation context routes it |
| 31 | "Build me a pack of skills for a customer-support engineer at a software company." | ✅ yes | Whole-pack design-and-build from a role — the Entry — Pack core case |
| 32 | "skillwright pack — research what a technical writer needs and build the roster." | ✅ yes | Direct pack invocation with domain research |
| 33 | "What skills are in the foundation pack?" | ❌ no | Roster lookup, not a design or build — the manifest answers it |
| 34 | "Build me a starter pack of prompts for cold sales emails." | ❌ no | A pack of prompts, not skills — promptwright's job; "pack" alone doesn't route here |
| 35 | "Humanize the README in my skill pack — it reads like a bot wrote it." | ✅ yes | Prose pass on a pack's own file; the object is named in the description and commwright's humanize clause is scoped to a message |
| 36 | "The prose in my pack's CLAUDE.md is unreadable — rewrite it, same rules." | ✅ yes | Prose pass; tokenwright also names CLAUDE.md but claims it for footprint ("without changing behavior"), and nothing here asks for a trim |
| 37 | "Humanize the release announcement for this skill before I post it." | ❌ no | A message bound for a channel is commwright's even when a skill is its subject — the prose clause claims files, not audiences |
| 38 | "Audit this skill for security problems before I publish it — secrets, injection, anything unsafe." | ✅ yes | Security pass on a skill package; the description's security clause names all three cues |
| 39 | "Does my SKILL.md tell the model to just do whatever the files it loads say?" | ✅ yes | S-1 injection surface in a skill's own instructions — the artifact is a SKILL.md, named in the trigger list |
| 40 | "Check the template my skill generates — I think it writes world-readable config and pins nothing." | ✅ yes | S-4 unsafe defaults in generated output; the object is what a skill package bakes in, not a running system |
| 41 | "Review my nightly agent's tool permissions." | ❌ no | Runtime permission on an autonomous agent — agentwright's, by the boundary sentence; no skill package is in play |
| 42 | "My scheduled agent reads customer email — how do I stop an injection from making it send things?" | ❌ no | Injection at runtime with a blast radius — agentwright's isolation and trust-tier job; skillwright's injection claim is scoped to a skill's own instructions |
| 43 | "Pen-test our API for injection vulnerabilities." | ❌ no | Code-level threat coverage — a security harness's job, neither wright's; "injection" alone does not route here |

**Security-pass edge notes (v1.4.0).** #38–#40 vs #41–#42 is the whole risk of the added clause, and the word doing the damage is **injection**: it now appears in skillwright's description *and* in agentwright's, so the object decides and nothing else. A **skill package** — its SKILL.md, its reference files, the template it generates — is skillwright's; a **running agent** — its permissions, its cadence, what an injected instruction could make it do — is agentwright's, which claims untrusted content flowing through an agent by name. #42 is the sharpest pair-mate to #38 (same noun, opposite object) and must be judged first on the re-run, #41 second. #43 sits outside both descriptions on purpose: agentwright already defers code-level threat coverage to a security harness, and skillwright names no code object at all — a row that fires either wright is a breadth defect in whichever fired.

**Prose-pass edge notes (v1.3.0).** #35/#36 vs #18 is the whole risk of the added clause: a README *belonging to a skill or pack* routes here, a README for a Python library does not, and the only thing separating them is the ownership qualifier "a skill's or pack's own files" — #18 is now the closest no-row in the suite alongside #8 and must be judged first on the re-run. #37 is the seam's message side and the line the clause must not cross: same verb, same subject, different object class. #36 vs tokenwright is the CLAUDE.md contention the edit created — a second description now names that file, and only the cost cue separates them.

**Edge notes.** #31 vs #34 is the pack boundary — a pack of *skills* routes here; a pack of *prompts* is promptwright's even with identical phrasing. #33 marks lookup vs build. #28 vs #29 is the integrate boundary — pack/roster/member vocabulary routes here; "integrate" against an app or API does not. #30 only fires when a pack build just offered the continuation.

**Original edge notes.** #1 vs #11 is the sharpest boundary — "build a skill that does X" wants a package; "write a prompt that does X" wants promptwright. #8 and #17 both mark the brandwright boundary — applying a brand and inventing one alike route there; skillwright builds neutral. #24 is the port near-miss — "port" alone is ambiguous; the description anchors it to skill sets. #26 vs #25 marks the evalwright line — a suite for a skill being built ships with the build; a suite for an existing skill routes to evalwright. If real usage misses the yes-set, push the trigger verbs harder; if it fires on the no-set, tighten the boundary sentences.
