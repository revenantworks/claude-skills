# Pack Registry — structural single source of truth *(event-driven surface)*

## Contents

- Build defaults
- The pack registry
- Per pack: members, budgets, seams (foundation), capstone, canonical repo — foundation, then gamedev

> The canonical roster and pack structure `tools/build.py` derives every
> `references/pack.md` from. **Event-driven** — updated on roster or pack
> changes only, not on a calendar (no staleness stamp). Holds only what a
> *build* needs to name and stamp a member. Brand **styling** — palette, voice,
> taglines, wordmark, firewall — does **not** live here; it lives in
> brandwright's `brand-definition.md` and is applied only when brandwright is
> invoked. The token below is a **label** (it names skills and stamps
> frontmatter); it applies no styling on its own.

## Build defaults

| Parameter | Value |
|---|---|
| Brand token *(label)* | `revenantworks` — names skills (`<brand>-<pack>-<skill>`) and stamps `metadata.brand`; a label, not applied styling |
| Naming template | `<brand>-<pack>-<skill>` (64-char guard) |
| License default | MIT |
| Packaging default | Folder-zip, full files including `evals/`, folder at top level, named `<n>-<version>.zip` — `.skill` on request |

## Pack registry

| Pack | Profile | Notes |
|---|---|---|
| `foundation` | standalone | Lean, no tools beyond web search, low overhead; self-updating stamps. Conformance checks (2026-07-13): C-1 drift-audit verb · C-2 neutral default. Integrate policy: restamp: lazy (2026-07-13) |
| `gamedev` | standard | Game-development skills on the `-smith` motif (registered 2026-08-29, first member pixelsmith). Standard profile because image viewing is an optional, declared capability with a stated text path; no scripts, no packages, no network at runtime. Conformance checks (2026-08-29): C-1 drift-audit verb · C-2 neutral default. Integrate policy: restamp: lazy (2026-08-29) |
| `localops` | standard | Skills for running work on local models, on the `-runner` motif (registered 2026-09-09, first member lmstudiorunner). Standard profile because it calls a local HTTP API at runtime — a declared, local-only dependency with a stated no-shell fallback; no scripts, no packages, no cloud network. Conformance checks (2026-09-09): C-1 drift-audit verb · C-2 neutral default. Integrate policy: restamp: lazy (2026-09-09) |

**foundation members** *(canonical roster — pack manifests are generated from this table)*

| Member | Job | Route there when |
|---|---|---|
| `revenantworks-foundation-promptwright` | Builds, scores, and hardens prompts with model-tier routing, and grills a request before the build until nothing essential is left to guess at | The deliverable is a prompt, meta-prompt, or system prompt — or a request that must be pinned down before one is written |
| `revenantworks-foundation-skillwright` | Builds, audits, and ports Agent Skills and packs | The deliverable is a skill, SKILL.md, or pack — new, audited, or ported |
| `revenantworks-foundation-commwright` | Shapes messages per channel, audience, and applied voice; audits message drift | The deliverable is a message, announcement, comms plan, or message audit |
| `revenantworks-foundation-agentwright` | Designs and audits autonomous agent systems — guardrails, trust tiers, kill switches | The deliverable is an agent's operating spec or an audit of one |
| `revenantworks-foundation-lorewright` | Research-verified verdicts and playbook reference docs with evidence tags | The deliverable is a recommendation or a reference document |
| `revenantworks-foundation-brandwright` | Defines brand and voice; applies them on request; audits repos, packs, docs, and artifacts for drift; exports payloads and the HTML brand-guide card | The deliverable is a brand or voice definition, its application, a drift report, or an export |
| `revenantworks-foundation-evalwright` | Authors and audits eval suites — build-time generator, zero runtime dependency | The deliverable is a trigger-eval set, assertion suite, or suite audit |
| `revenantworks-foundation-tokenwright` | Measures, budgets, and slims the token footprint of LLM-facing artifacts | The deliverable is a leaner artifact, a token-efficiency audit, or a budget sheet |
| `revenantworks-foundation-rigwright` | Builds the standing configuration Claude reads before work — Project instructions, knowledge plans, CLAUDE.md, repo Claude config | The deliverable is attended standing config for a Claude surface |
| `revenantworks-foundation-dispatchwright` | Runs a session's fan-out — decomposes, tiers via promptwright, dispatches with a durability contract, and reconciles against origin | The deliverable is a large request split into dispatched units, or a stalled/finished fan-out that needs resuming or auditing |
| `revenantworks-foundation-resumewright` | Writes a committed session handoff — state verified against origin, decisions, the ordered remainder, and the resume procedure — on request or before a pause, and commits it in the same call | The deliverable is a session handoff, a paused session that needs to resume from a committed record, or a request to hand off before a usage-limit or compaction pause |

**foundation budgets** *(body-footprint table — one row per member; `build.py` validates each member's measured SKILL.md body against its row)*

Declared here rather than in each member's frontmatter, changed 2026-07-27. The gate is unchanged — a body's size must be **declared and justified**, per the item-③ rescope — but the justification is build-time data, and frontmatter is loaded on every single invocation of the skill. A `why` in `metadata.body_budget` cost 51–95 tokens per member *every time that member ran*, to explain a number no runtime reader ever acts on. That is the exact waste tokenwright exists to find, sitting in the pack's own gate. The registry is already `build.py`'s single source of truth, no member loads another member's registry, and nothing here ships in a member's zip — so a row costs zero runtime tokens. **That is what makes declaring all eight affordable**: eight frontmatter blocks would have cost ~400 tokens per session to say that five members have no size problem.

**Standalone skills built outside a pack keep `metadata.body_budget` in frontmatter** — they have no registry to declare into, so the frontmatter form stays valid and `build-templates.md` still teaches it. It is the pack-member case that moves.

| Member | Tokens | Why it earns the room |
|---|---|---|
| `revenantworks-foundation-agentwright` | 5300 | ten control areas, five runtime classes and the trust-tier rule are named in the body and detailed in references; **raised 2900 → 3500 at 1.3.0 (2026-07-30)** — `Entry — Emit` is a fifth entry point whose enforcement-gap step and three invariants are decision rules, so they are body-resident like the other four; still under the advisory; **raised 3500 → 3800 at 1.2.4 (2026-09-09)** — the estate's foundation-fetch-members-name-no-domain-allow-list finding put a named fetch-scope statement on Entry — Refresh, a decision rule (which hosts this run may verify against) body-resident for the same reason the rest of the entry is; landed at ≈3704; **raised 3800 → 4100 at 1.2.5 (2026-09-09)** — Entry — Audit gained an inventory-mode paragraph (scheduled-task provenance, the agentwright half of a rigwright/agentwright split); a decision rule stating when the audit's scope widens, body-resident for the same reason the rest of the entry is; landed at ≈3955; **raised 4100 → 4900 at 1.2.8 (2026-09-11)** — Entry — Audit gained three questions the checklist areas do not ask (a structurally unreachable fallback logs a failure, the session count matches the cron, the run log's first result event is the fire's completion evidence), a delegation line naming the surface that can reach a routine's own API, and the extent-parsed rule for a scored config list (task-observer observations #0017, #0018, #0033, #0041); `references/security-scan-doctrine.md` also scores a scanner's findings by file role (#0031) — reference-resident, but the pointer and the four Audit rules above are body-resident by this row's own standing logic; landed at ≈4780; **raised 4900 → 5300 at 1.2.9 (2026-09-13)** — the weekly review landed four decision rules a design or audit run must not open a file to know: the boundary fork before an unattended agent's platform or creation type is chosen (#0049), the stop order and the one-minute absence proof for a kill switch (#0051), hold-and-announce at a terminal date (#0052), and the regeneration-guard question before any cleanup rule (#0053); body-resident by this row's own standing logic; landed at ≈5111 |
| `revenantworks-foundation-brandwright` | 5000 | the neutral-core law and both export shapes state their contracts inline; doctrine files hold procedure; **raised 3300 → 3450 at 1.2.0 (2026-08-07)** — multi-definition support added a *Which definition* selection block, and selection is always-relevant routing: a run must know which brand it is holding before it opens anything, so the resolution order and the cross-brand law are body-resident like the other laws. The slimmed block landed at 3296 against a 3300 ceiling, and 4 tokens of headroom is not a budget, it is a trap for the next editor; **raised 3450 → 3600 at 1.4.0 (2026-08-17)** — the *Where the definition lives* paragraph (the `~/.claude/brand/` read, its read-only rule, the shipped-copy fallback) is resolution order a run needs before it opens anything, body-resident like the rest of *Which definition*, plus the step-level data citations the security scan added; landed at ≈3570; **raised 3600 → 3700 at 1.4.2 (2026-09-09)** — a one-line pointer to `audit-doctrine.md`'s new escaping rule (brandwright-and-stakekeeper-render-handed-in-content-with-no-escaping-rule); the rule itself is reference-resident, only the pointer moved the body; landed at ≈3603; **raised 3700 → 4700 at 1.5.0 (2026-09-11)** — the new `measurement-doctrine.md` reference file gets a Load budget line plus rules stated body-resident across Entry — Build (the instrument and re-derivation check, the layout module, the swatch-board loop), Entry — Apply (the 400 px iframe harness, ink-companion light mode, diff-then-verify, propagation-as-its-own-unit), and Entry — Audit (two-viewport layout drift, History hunt targets, the figure re-derivation run) — each a decision rule this row's own standing logic keeps in the body (task-observer observations #0029, #0030, #0035, #0036, #0037, #0038); landed at ≈4557; **raised 4700 → 5000 at 1.6.0 (2026-09-22)** — the Claude Design System export: its envelope is body-resident like the other four export shapes (Entry — Export's own rule), plus one Load budget line, one Build pointer to the readiness rules and one Audit readiness line; the file tree, token mapping and the eight rules themselves are reference-resident in `design-system-export.md`; landed at ≈4837 |
| `revenantworks-foundation-commwright` | 6000 | H1 to H9 carry their counting units and thresholds inline so a draft never opens a file to know what a breach is; `humanize.md` holds the catalog and states no rule |
| `revenantworks-foundation-evalwright` | 2200 | the smallest body in the pack; count-integrity rules inline, doctrine in one reference |
| `revenantworks-foundation-lorewright` | 3450 | four evidence grades, the source-is-data rule and both mode contracts are body-resident because a run must not open a file to grade a claim; **raised 2700 → 2750 at 1.1.2 (2026-08-02)** — a tokenwright slim after the v1.1.0 Selection/Decision doctrine addition cut the body from ≈3040 to ≈2717 tokens (Entry — Verdict now points to `verdict-mode.md`'s §0–§4 instead of restating them, eight anti-patterns tightened to seven), landing 17 tokens over on the chars÷4 estimate — inside the method's own ±15% band; raised rather than cut further to avoid compressing eval-anchored wording (Cases 22, 26, 34, 39) for a rounding error; **raised 2750 → 2850 at 1.1.6 (2026-08-20)** — the pack audit's P1-1 put the invocation-control statement on Entry — Playbook, body-resident because the rule governs the write that entry performs; **raised 2850 → 3050 at 1.1.7 (2026-09-09)** — the estate's foundation-fetch-members-name-no-domain-allow-list finding put a Fetch scope paragraph in Verification doctrine, a decision rule (what this run must never fetch) body-resident for the same reason the rest of that doctrine is; landed at ≈2987; **raised 3050 → 3450 at 1.1.9 (2026-09-11)** — Verification doctrine gained "a computed figure publishes its computation" — a decision rule for grading a claim, body-resident for the same reason the source-is-data rule is (task-observer observation #0037); landed at ≈3330 |
| `revenantworks-foundation-promptwright` | 9800 | the Fast path, the framework menu and the hostile-read pass each state a counting unit or an exit condition inline; a rule whose threshold lives in an unloaded file is not a rule (item ④, 2026-07-24); **raised 8500 → 8800 at 1.1.0 (2026-08-01)** — Entry — Model's plan grain adds the target-table delivery shape and the living-table and standing-rule contracts, decision rules body-resident like the rest of the entry; reason stated in the member's CHANGELOG; **raised 8800 → 8850 at 1.2.0 (2026-08-02)** — Tier routing's two role-based overrides (planning-effort, cross-model review) are decision rules governing every subtask, body-resident for the same reason as the rest of the entry; landed at ≈8805, tightened once, raised rather than compressed further into a two-sentence addition; **raised 8850 → 8860 at the 2.0.0 rename (2026-08-07)** — the member name grew 5 chars (`revenant` → `revenantworks`) and appears in the body, moving the chars÷4 estimate to ≈8851; bookkeeping for the rename, no content change; **raised 8860 → 9030 at 1.4.0 (2026-08-12)** — the estate audit put the fetched-page injection rule and the search-unavailable no-restamp fallback into Entry — Refresh (findings 2, 14), security rules that are body-resident by the same logic as the rest of the entry; landed at ≈8977; **raised 9030 → 9470 at 1.5.0 (2026-09-09)** — Entry — Grill is a new entry point, and an entry's route contract (which phase it replaces, where the ladder resumes, the two shapes, the agentwright seam) is body-resident like every other entry here; its focus areas, indicators and round procedure are **not** — those live in `grill.md` and were deleted from the body in the same pass, which is why the raise is 440 and not the section's full cost; landed at ≈9439; **raised 9470 → 9800 at 1.5.8 (2026-09-11)** — Phase 5 Tier routing gained a role-based override for a verifier that only re-derives evidence already on disk, and Entry — Model's plan grain states a fan-out row's agent count and the effort those agents inherit (task-observer observation #0022) — both decision rules governing every plan table, body-resident for the same reason the rest of the entry is; landed at ≈9638 |
| `revenantworks-foundation-skillwright` | 9450 | eleven entry points and the pack/integrate/release contracts state their gates and count-integrity numbers inline; the references hold catalogs and templates, not rules; **raised 7800 → 8080 at 1.2.0 (2026-08-12)** — the estate audit added the fetch injection rule to Build step 3 and Entry — Refresh and the shell/python3 packaging declaration (findings 2, 17), partly paid for by re-homing Packaging step 4's caps to Rubric A (finding 15); landed at ≈8027; **raised 8080 → 8180 at 1.2.1 (2026-08-14)** — the estate audit put the same rule on Entry — Upkeep step 1, which reads other skills' files and then acts on what it read; body-resident for the same reason the other two are, and `upkeep-doctrine.md` carries a pointer rather than a second copy; landed at ≈8101; **raised 8180 → 8300 at 1.3.1 (2026-08-17)** — the security scan put the data-never-instructions rule on Build step 1 (mined conversation and attachments) and Integrate step 1 (registry rows and sibling files) and named hidden text in the security pass; landed at ≈8172, and 8 tokens of headroom is a trap for the next editor, not a budget; **raised 8300 → 8450 at 1.3.3 (2026-08-20)** — the pack audit's P1-1 put the invocation-control statement in Behavior notes and its P1-2 replaced the false "at most two" load-budget sentence with the real per-entry count, both body-resident because a build must not open a file to know what it loads; landed at ≈8330, and the headroom this time is deliberate; **raised 8450 → 9100 at 1.3.9 (2026-09-11)** — Packaging step 4 now measures every description in the delivery set, Packaging gained step 5 (run the leak guards against the staged tree after `git add -A`), and Entry — Audit gained a Third-party adoption paragraph scoring a scanner's findings by file role — all decision rules a build must not open a file to know (task-observer observations #0011, #0031, #0034); landed at ≈8969; **raised 9100 → 9450 at 1.4.0 (2026-09-13)** — Build step 4 partitions candidate content by reach before returning a verdict and Restraint gained the placement-call condition, Entry — Audit gained a usage-evidence probe rule, and its Third-party adoption paragraph gained the group-by-matched-string step with the coverage-denominator corollary — all decision rules a build or audit must not open a file to know (task-observer observations #0055, #0059, #0060); landed at ≈9401 (measured by `build.py --footprint` at apply time; the draft's estimate was 9304) |
| `revenantworks-foundation-tokenwright` | 4200 | the waste taxonomy's classes and the lossless/lossy ladder are decision rules, not catalogs |
| `revenantworks-foundation-rigwright` | 5600 | the seven-layer placement stack states its own decision rule inline because a placement question must cost no reference load; the references hold surface facts and templates, not rules; **corrected 2400 → 2900 (2026-07-30)** — the 2400 was authored before anything was measured and the as-built body is 2537, so the row was an estimate the member never met, not a ceiling it drifted past; **raised 2900 → 3300 at 1.1.3 (2026-09-09)** — Entry — Audit gained an inventory-mode paragraph (skill/hook provenance, the rigwright half of a rigwright/agentwright split); a decision rule stating when the audit's scope widens, body-resident for the same reason the rest of the entry is; landed at ≈3115; **raised 3300 → 3500 at 1.1.4 (2026-09-10)** — Placement — the layer stack gained the live/tracked-pair diff rule (task-observer observation #0003), a decision rule stating when to diff a config's live and tracked copies before editing either, body-resident for the same reason the other layer-stack rules are; landed at ≈3364; **raised 3500 → 4700 at 1.1.7 (2026-09-11)** — Entry — Audit gained a usage-evidence rule (name the counter, state its blind spot, score into three buckets), the owner-install boundary for a catalog row touching a live hook/permission file/`.mcp.json`/cap/baseline, a positive-control check on a gating pattern list, the extent-parsed rule for a scored config list, and the newly-written-check-is-an-untested-claim rule — five decision rules Entry — Audit runs on every pass, body-resident for the same reason the rest of the entry is (task-observer observations #0016, #0024, #0026, #0039, #0041, #0042); landed at ≈4570; **raised 4700 → 5000 at 1.1.8 (2026-09-13)** — the weekly review landed two body decision rules — a stateful hook's controls are side-effect-free at check time and declare what they touch (#0047), and the partition-by-reach placement call that decides whether a rule lives in a repo's CLAUDE.md or a cross-project skill (#0059) — with the mixed-line-endings fact reference-resident (#0058); landed at ≈4888; **raised 5000 → 5300 at 1.1.9 (2026-09-13)** — a live-state-dependency rule (#0062): a fixture whose validity rests on a real flag or another hook's write declares that dependency and prefers pinning the exact state it needs over depending on whatever the real file currently says; landed at 4972, and 28 tokens of headroom against a 5000 ceiling is the same trap this row's own history already named once, so the raise covers the next decision rule too, not only this one; **raised 5300 → 5600, staged 2026-09-20** — the weekly review landed three more body decision rules: a fixture is literal in shape and tokens but never in paths, so a user-profile path is rewritten before it is committed (#0063); a claim about the current state of work is the wrong layer for standing config, which names the one status file and restates nothing (#0080); and where two accounts share a rig, the permission layer records per-identity routing and which steps that leaves controller-run (#0088) — body-resident by this row's own standing logic, the same logic every raise above rests on; measured at ≈5490, so the row it was governed by was already 190 short when the rules landed. Installed 2026-09-22, when the number cell was moved to match: the staged copy had changed only this note, so the build kept reading 5300 |
| `revenantworks-foundation-dispatchwright` | 7200 | measured ≈3254 at 1.0.0 build (`build.py --footprint`) — well under the 5k advisory, declared anyway from first release per the precedent that starting declared costs nothing at runtime and removes the choice later. The nine numbered sections (scope, shape check, decompose, tier, durability contract, wave execution, escalation, reconcile, anti-patterns) state their own caps and thresholds inline — six concurrent units, two nesting levels, the atomic-commit-plus-push rule — because a fan-out run must not open a file to know its own limits. Ceiling set ~22% above the measurement for headroom on a first content pass; **raised 4000 → 4500 at 1.2.2 (2026-09-10)** — §6 Wave execution gained a silence-is-not-a-liveness-signal bullet and §8 Reconcile gained gate-figure and test-total re-derivation bullets (task-observer observations #0006, #0007, #0008), decision rules a unit must not open a file to know, body-resident for the same reason the other eight sections are; landed at ≈4266; **raised 4500 → 4700 at 1.2.4 (2026-09-10)** — §5 Durability contract gained a ledger/RESUME-committed-at-every-write rule and a resume-time `git stash list` check, and §6 Wave execution gained a count-agents-not-rows rule for the wave cap and usage window (task-observer observations #0022, #0032); all three are decision rules a run must not open a file to know, body-resident for the same reason the other caps are; landed at ≈4638; **raised 4700 → 4900 at 1.2.5 (2026-09-11)** — §1 gained a fourth seam bullet naming the new sibling resumewright, quoting its own boundary line back per this section's existing pattern for every other seam bullet, and the `resume` entry gained a one-sentence pointer; body-resident for the same reason the other three seam bullets in this section already are — a run reading §1 must see the full boundary set without opening a second file; landed at ≈4820; **raised 4900 → 5700 at 1.2.6 (2026-09-11)** — §2's shape check now counts surfaces rather than verbs, §3 Decompose gained three rules (a unit's tool list, a cross-repo finding split before dispatch, the packet id/brief join), and the durability contract now states a unit's gate runs against the staged tree — decision rules a fan-out run must not open a file to know, body-resident for the same reason the other eight sections are (task-observer observations #0016, #0019, #0020, #0023, #0026, #0028, #0033, #0034, #0035, #0036); landed at ≈5547; **raised 5700 → 6400 at 1.2.7 (2026-09-13)** — the weekly review landed nine observations, six of them body decision rules: helper isolation in Wave execution (#0043/#0044), the reversal check in Reconcile (#0045), the boundary fork in Shape check (#0049), stop-order cancellation (#0051), the regeneration-guard question (#0053) and the asserted match count in the durability contract (#0058); procedure went to the brief and the ledger schema, and the raise covers only the rules a fan-out run must hold without opening a file; landed at ≈6203; **raised 6400 → 6700 at 1.2.11 (2026-09-14)** — 1.2.9's self-contained tiering took the body to ≈6400 with no raise, and 1.2.11's §5 merge-only-onto-a-green-base rule (#0077) is a durability decision a unit must hold without opening a file; landed at ≈6454; **raised 6700 → 7200 at 1.3.0 (2026-09-17)** — the owner's window-fit request: the plan entry now ends on one fitted table and a stop, §2 names that table as the shape a plan must end in, §6 carries the window-fit decision rules (read the file or ask, never guess; a percent becomes tokens only through measurement; split at unit boundaries; a unit larger than a window is a decomposition defect) and §8 the calibration write-back — decision rules a plan turn must hold without opening a file, while the procedure, the file schemas and the fit algorithm went to `references/window-fit.md`; landed at ≈6952 |
| `revenantworks-foundation-resumewright` | 3200 | measured ≈1906 at 1.0.0 build (`build.py --footprint`) — well under the 5k advisory, declared anyway from first release per the same precedent dispatchwright's own row states. Gather, Write, Commit and the Never list each state their own rule inline (verify against `git` not a report; commit in the same call; never installs its own trigger) because a handoff run must not open a file to know what it is bound by; the one reference file holds the section shape and the resume-time checks, not rules; **raised 2600 → 3200 at 1.1.0 (2026-09-14)** — 1.0.3 and 1.0.4 took the body to ≈2611 with no raise, and 1.1.0's Write step 4 (emit the paste-ready starter prompt) plus its Never bullet are decision rules a handoff run must hold without opening a file, while the prompt's block and rules live in the template; landed at ≈2895 |

**foundation budget notes:** the three members over the 5k advisory (commwright, promptwright, skillwright) keep the numbers they were released with — **this move is not a ratchet**, and raising a ceiling still needs a content reason stated in the member's CHANGELOG. `build.py --footprint` prints the measured bodies against these rows; run it before any content edit on skillwright, which sits inside 100 tokens of its ceiling, or promptwright, which ran that close until the 1.1.0 raise and drifts back toward it with every body edit. The five members added here were previously undeclared because they are under the advisory, which is why the pack had no single comparable number across members — an accepted downside the item-③ rescope recorded, and the one this table pays back.

**tokenwright freeze note** (added 2026-09-10, estate finding `tokenwright-freeze-policy-single-homed-in-changelog`): **Frozen member — owner decision, 2026-08-17: no version bumps on tokenwright unless something is actually broken.** The rule previously lived only in tokenwright's own `CHANGELOG.md` ([1.2.2], [1.2.3]), a file this pack's own doctrine treats as frozen history and neither upkeep nor integrate reads for standing policy — so an upkeep sweep that finds `references/measurement.md` past its 60-day calendar cadence had nothing here telling it the member is frozen. Recorded here so upkeep and integrate both see it before proposing `tokenwright refresh`. **Whether a calendar-cadence refresh (a volatile-surface restamp with no content change) counts as "broken," and is therefore exempt from the freeze, is an OPEN owner decision, not decided by this note** — an upkeep run that reaches `measurement.md`'s overdue date should surface the conflict and ask, never restamp on its own read of "broken."

**foundation seams** *(routing-seam table — one row per boundary pair; pack manifests are generated from this table too)*

| Seam | Left owns | Right owns | Router keys on | Cold-listing signal |
|---|---|---|---|---|
| promptwright ↔ tokenwright | What the prompt says and how well it performs — wording, structure, hardening, scoring, and the run-target tier pick | What the artifact costs — behavior-preserving trims, budgets, cached-prefix fit, waste audits | The stated motive. A cost cue (cheaper, too many tokens, fits the budget) is tokenwright; a quality cue (works better, stop the rambling, fix the output) is promptwright. A bare "tighten this prompt" carries neither and stays promptwright — the trim carve-out only sends work away when behavior is explicitly held constant | both descriptions |
| skillwright ↔ lorewright | Whether a skill or pack idea fills a real niche — the verdict on a skill, however heavily sourced | Sourced, evidence-graded go/no-go and comparison verdicts on everything that is not a skill | The object under judgement, not the sourcing verb. "Is there a niche for X" stays skillwright even asked with sources and a competitor scan, because lorewright defers skills by name; research framing pulls lorewright-ward without moving the seam | both descriptions |
| skillwright ↔ brandwright | Executing a change across a skill set — port, rename, rebrand, sanitize for a new owner — and the propagation that follows (registry rows, manifests, packages) | Defining a brand or voice, applying it on invoke, auditing a repo or skill set for drift, exporting payloads and the guide card | Whether identity moves or only styling. Names, metadata, manifests, packages change → skillwright port; palette, taglines, wordmark, or voice land on artifacts that keep their names → brandwright apply. Recorded edge: a full-pack reskin foregrounding palette and taglines straddles — port when the set's names or manifests move, with brandwright supplying the definition; apply when they do not. A drift audit is brandwright either way | both descriptions |
| commwright ↔ brandwright | Applying a voice to the message in hand — one message out, the stored profile untouched | The voice itself — defining, saving, editing, restyling, or exporting a stored profile | Whether anything durable is created or changed. A named message is commwright; making a way of writing standing ("how we always write") is brandwright even when it arrives as "my commwright voice" or as a commwright subcommand | both descriptions |
| evalwright ↔ skillwright | Authoring, scoring, and refreshing a suite for a target that already exists | The trigger evals that ship inside a build — a build artifact, not a suite request | Whether a build is in play in the same breath. Evals asked for against an existing skill, prompt card, or agent spec are evalwright; evals that appear because a skill is being built belong to that build, authored under evalwright's doctrine when it is installed. Closed on skillwright's side too — its description now hands over "authoring or scoring an eval suite as its own job" to evalwright by name | both descriptions |
| dispatchwright ↔ promptwright | Turning a request already judged worth fanning out into dispatched units, briefed, wave-launched, and reconciled against origin | Which tier, model, and effort each unit runs on — the target table dispatchwright copies verbatim | Whether the ask is decomposition-and-execution or a tier pick. dispatchwright never invents a model; it hands the finished unit list to promptwright's Entry — Model plan grain and copies back what it returns. Declared on dispatchwright's side only (SKILL.md §1, quoting promptwright's own decomposition-is-the-caller's limit back at it) — promptwright's description names no fan-out sibling, so the signal reads one description | one description |
| dispatchwright ↔ rigwright | Decomposing, tiering, dispatching, and reconciling a fan-out already judged worth running | Placing the hook or CLAUDE.md rule that makes a big request reach for dispatchwright at all | Whether the request is being run or the trigger for it is being wired. dispatchwright runs the fan-out and never places its own hook. Declared on dispatchwright's side only (SKILL.md §1) — rigwright's description names no dispatch sibling | one description |
| dispatchwright ↔ agentwright | A one-time (or resumable) session fan-out with the model watching every unit land | Anything firing on a schedule or event with nobody reading the result | Attended or not. A fan-out dispatchwright runs is watched to completion in the same session; a cron job or unattended routine is agentwright's whole, and dispatchwright never runs on one. Declared on dispatchwright's side only (SKILL.md §1) — agentwright's description names no dispatch sibling | one description |
| dispatchwright ↔ resumewright | Resume state for an active, ledger-tracked fan-out — the units, tiers, and durability contract a dispatch run is already running under | A committed session handoff for an ordinary session, dispatch or not — state verified against origin, decisions, the ordered remainder, the resume procedure | Whether a dispatch run's own ledger is already open. Inside an active fan-out, dispatchwright's own `resume` entry and ledger already cover the shape; resumewright is for everything outside that — a single-repo session, or a paused session before one starts. Declared on both sides (dispatchwright 1.2.5 SKILL.md §1 and its `resume` entry; resumewright's own description) | both descriptions |
| agentwright ↔ promptwright | The system around an autonomous or scheduled agent — guardrails, trust tiers, kill switches, cadence, retries, output contracts, isolation of untrusted input | The agent's prompt text itself — its instructions, meta-prompt, or system prompt, and the tier it runs on | Prompt text or the machinery around it. agentwright claims everything but the prompt text and says so; promptwright claims agent and bot instructions. A harden-my-agent request with no prompt in hand is agentwright; hand over the instruction block and it is promptwright. Code-level threat coverage is neither — it belongs to a security harness | both descriptions |
| promptwright ↔ lorewright | Which model or tier to run a given prompt or live task on — a run-target pick | A sourced, evidence-graded comparison of models as products — pricing, capability, vendor commitment | Whether there is something to run. A named task or prompt in play is promptwright; a standardize-on-one decision demanding sources with nothing to run is lorewright. Watched pair since 1.1.0 — promptwright carries the carve-out, lorewright's side claims comparisons generally and never distinguishes a run-target pick | one description |
| skillwright ↔ agentwright | The skill package as built — injection surface in its own instructions, secrets in the artifact, undeclared or ungated capability, unsafe defaults in what it generates; run as a named security pass inside every Entry — Audit, never as its own verb | What an autonomous agent may do when it runs — tool-grant scope, untrusted-content flow, guardrails and kill switches, credentials, failure and retry as exposure; run as Entry — Security-scan against the five runtime classes | The object, not the vocabulary. `injection` sits in both descriptions, so it decides nothing: a package on disk — SKILL.md, its reference files, the template it generates — is skillwright; a running agent — its permissions, cadence, blast radius, what an injected instruction could make it do — is agentwright, which claims untrusted content flowing through an agent by name. A finding that crosses is handed over by name, never filed on the wrong side. Code-level threat coverage is neither and both say so | both descriptions |
| skillwright ↔ commwright | Prose-style work on repo files — SKILL.md, README, CHANGELOG, SOURCES, reference docs, CLAUDE.md, spec files: the artifacts skillwright generates, audits, ports, scores, and rewrites for register under Entry — Audit's prose pass | Prose-style work on messages — anything addressed to an audience through a channel, the only text commwright's humanize rules bind | Where the text lives, not the verb. A file at a repo path is skillwright; a message bound for a channel is commwright, and the same content pasted into a release body or a post is a message again. From a cold listing the deciding text is skillwright's positive file claim — "a prose pass on a skill's or pack's own files (README, CLAUDE.md, docs)" — because commwright claims messages generally and it is its type contract ("any message to its channel and audience") that a file object fails, never a clause naming a file. commwright's body refuses the file case too ("never a pack's own docs, a SKILL body, or any other file it is pointed at"), but a body is read only once that member is already in play | both descriptions |
| rigwright ↔ agentwright | Standing configuration a human reads in session — Project instructions, knowledge plan, CLAUDE.md, `.claude` layout, `.mcp.json` | Anything firing on a schedule or an event with nobody reading the result — the task or routine prompt, its cadence, its guardrails, its zero-signal line, and the surface it is emitted into | Who reads the output, never the filename. A Claude Code desktop scheduled task is stored on disk as a `SKILL.md` and is agentwright's, because a filename names a format and not an object. "Set up" appears in both descriptions and decides nothing; the object test is what both sides carry | both descriptions |
| rigwright ↔ skillwright | The config a session opens with — always loaded, charged on every turn whether or not it is needed | The skill package — loaded only when relevant, and how the package itself is built, audited, and ported | Whether the artifact is always-on or on-demand. This is the same every-session/some-sessions test rigwright's own layer stack applies internally, so the seam and the doctrine cannot drift apart. A `SKILL.md` under `.claude/skills/` is skillwright's even though rigwright emits the tree around it | both descriptions |
| rigwright ↔ promptwright | Which layer a rule belongs in and what budget binds it there | The wording of the instruction block once its home and budget are settled | Placement or prose. A named surface carrying a layer question is rigwright; a block in hand with a quality cue is promptwright. Recorded edge, watched from birth — a Project instruction block is close to a system prompt, and rigwright's named-surface claim is what beats promptwright's generic one, the same way skillwright's file claim beats commwright's message claim | both descriptions |
| rigwright ↔ tokenwright | The config surface itself — which layer a rule belongs in, whether the setup is bloated or drifted, and any trim that follows from placement | The token cost of any artifact, config files included — behavior-preserving cuts, budgets, cached-prefix fit, waste audits | The stated motive, the same key as promptwright ↔ tokenwright. A placement, bloat, drift, or setup cue is rigwright; a pure cost cue with behavior held constant is tokenwright. A bare "trim my CLAUDE.md" names the surface — a setup cue — and stays rigwright | both descriptions |

| promptwright ↔ skillwright | The prompt artifact — writing, scoring, hardening, red-teaming a prompt, meta-prompt, template, or system prompt, wherever it will live | The skill package — building, auditing, porting, packaging a SKILL.md and its files | What the deliverable IS. A prompt destined to sit inside a skill is still promptwright until it is being packaged; a SKILL.md in hand, even one that is mostly prompt text, is skillwright — promptwright's description defers skill packages by name and skillwright's defers prompts by name (row added 2026-08-15; the reciprocal pair predated it undeclared) | both descriptions |
| skillwright ↔ tokenwright | Best-practice conformance of the skill package — structure, description, disclosure, security, and any change that may move behavior | A pure token or cost cut on a SKILL.md that already conforms, behavior explicitly held constant | Whether behavior may move. Conformance work is skillwright even when it shrinks the file; a cost-only trim on a conforming skill is tokenwright — skillwright's description hands that case over by name and tokenwright defers conformance by name (row added 2026-08-15; the reciprocal pair predated it undeclared) | both descriptions |

**foundation seam notes:**

- **dispatchwright ↔ resumewright** (added 2026-09-11, with resumewright's 1.0.0 build): the
  newest member and its nearest neighbor. Both sides carry the claim from the first commit that
  adds either — resumewright's own description names the dispatchwright carve-out, and
  dispatchwright 1.2.5 adds a matching §1 bullet plus a one-sentence pointer from its own
  `resume` entry, so the row opens as **both descriptions** rather than the one-sided pattern the
  three original dispatchwright rows above started at.

- **evalwright ↔ skillwright** (row corrected 2026-08-18; underlying close was skillwright 1.2.0, 2026-08-12): the row long read "one description" on the claim that "the carve-out lives on evalwright's side only — skillwright's description names no eval sibling." That was true when the row was authored but stopped being true at skillwright 1.2.0, when finding 16 of that release added skillwright's own boundary clause — "for authoring or scoring an eval suite as its own job, evalwright" — to close its two nearest-neighbour gaps. The registry row was never updated to match, so the audit trail said "asymmetric" for six days after the asymmetry closed. Corrected here to **both descriptions**, matching the live text of both members' frontmatter `description` (verified by reading each SKILL.md directly, not by trusting the prior row). The lesson generalizes: a seam row is a claim about two files, and it goes stale the moment either file changes without a registry pass in the same commit — the same class of gap the skillwright ↔ agentwright note below exists to name.

- **dispatchwright ↔ {promptwright, rigwright, agentwright}** (added 2026-08-18): `build.py --check` flagged dispatchwright as "named in no routing seam" from its 1.0.0 release (2026-08-18) forward. The three edges are real and were never in doubt — dispatchwright's own SKILL.md §1 states them by quoting the sibling that owns the other half of each ("The tier table is promptwright's," "Where the trigger lives is rigwright's," "Unattended runs are agentwright's, whole") — but the rows were never added to this table, and the member's own README named the gap explicitly under "What did not land at 1.0.0" rather than leaving it silent. All three rows are now present above. Every edge is genuinely uncontested rather than negotiated: none of promptwright's, rigwright's, or agentwright's descriptions mention dispatchwright or fan-out dispatch by name, so nothing on their side claims the territory dispatchwright is ceding or taking — the split is dispatchwright unilaterally staying out of model-picking, hook-placement, and unattended scheduling, not a boundary either sibling had to give ground on. The signal column reads **one description** on all three for that reason, not as a mark against them: an uncontested edge stated on only one side is not the same failure mode as the evalwright ↔ skillwright row above, where the claim was actively wrong. The warning `--check` prints is therefore now informative rather than a real gap — it should be read as "three declared, uncontested edges," and should stop firing once the three rows above are live in a build.

- **skillwright ↔ agentwright** (added 2026-07-27, pack 1.3.0): closed a boundary that shipped one pass earlier claimed from one side only. agentwright 1.2.0 and skillwright 1.4.0 each took a security capability and each wrote a reciprocal boundary sentence into its own `description`, but the row itself was deliberately skipped that pass because adding it regenerates all eight manifests — so the seam existed in routing metadata and nowhere the table could see it. Recorded plainly because it is the class of gap this table exists to catch: `tools/build.py`'s boundary-pair check validates the rows that are declared and has no way to notice one that is missing, so a `--check` clean run was never evidence the seam was covered. The deciding word is **injection**, now in two descriptions at once — the first time any single security token has been — which is why the row keys on the object rather than the vocabulary and why skillwright trigger rows #38-#43 pair each security query against its opposite-object twin (#42 against #38 is the sharpest, #43 sits outside both on purpose). Both sides carry the claim, so the signal reads **both descriptions**; the cold re-run that would instrument it is owed and disclosed, not claimed.

- **rigwright ↔ tokenwright** (added 2026-08-05, AUDIT-2026-08-05): opened by rigwright's 2026-07-30 addition — "trim a … CLAUDE.md" (rigwright) and "slim, compress, or token-optimize … CLAUDE.md" (tokenwright) shared an object with no declared row, and the naming note above still counted two CLAUDE.md descriptions. The clause lands on tokenwright's side only (1.1.0) — rigwright's description had 72 chars of headroom against the house warn band and its named-surface claim is the stronger side, the same asymmetry as promptwright ↔ lorewright — so the signal reads **one description**. The cold re-judge of tokenwright's trigger rows against the amended listing is owed and disclosed, not claimed. **Closed from rigwright's side 2026-08-12** (estate audit, finding 9): rigwright 1.1.0's description now ends on the tokenwright cost-cut clause and tokenwright 1.2.0's config clause cedes bare trims, so the signal reads **both descriptions**; both members' cold re-judges are owed and disclosed in their suites.

- **`sanitize`** is object-decided: a skill set handed to a new owner is skillwright port, a brand identity swept across a tree is brandwright audit, a public-bound message is commwright's pre-publish redaction sweep.

- **skillwright ↔ commwright** (closed 2026-07-25 on one side, skillwright 1.3.0; scoped on the other 2026-07-27, commwright 1.2.6): skillwright's description now claims a prose pass on a skill's or pack's own files and names README and CLAUDE.md, and Entry — Audit carries the procedure that serves the claim, so "humanize the README" and "humanize my CLAUDE.md" have a positive file claim to land on instead of firing commwright. commwright's description's `humanize` clause now names the excluded objects (a README, CLAUDE.md, or reference doc is skillwright's prose, not commwright's), so both descriptions carry the boundary and the signal now reads **both descriptions**; commwright re-reasoned the three probes cold in its `evals/RESULTS.md`, discharging the routing re-run that close owed on the description surface. Two limits, recorded rather than papered over: the close holds in the pack-repo reading, since a README belonging to no skill or pack is claimed by neither side by design (it is skillwright trigger row #18), and the routing judgement behind it is authored, not instrumented — the re-run is owed.

- **Naming notes:** "prose" now appears in exactly one description (skillwright) and "style" still in exactly one (brandwright, "style guide"), where it means brand identity and never prose register; "CLAUDE.md" appears in three — tokenwright's, claimed for footprint only; skillwright's, claimed for wording only; and rigwright's, claimed for placement and setup (the count read two until 2026-08-05 — it predated rigwright's 2026-07-30 addition; corrected with the rigwright ↔ tokenwright row).

- **Third limit** (opened and closed 2026-07-25): the always-on router now carries the seam. `packs/foundation/CLAUDE.md` loads on every turn under `packs/foundation/`; it previously gave skillwright only "Build, audit, port, or pack a skill" while the table's one rewrite-flavoured cue was commwright's, so "humanize the writing in my pack's README" met a standing surface naming commwright and never skillwright. The row now reads "…or a register pass on a skill's or pack's own files" with the cue "humanize / tighten this README", and a How-they-compose bullet states the split: prose on repo files is skillwright's, prose bound for a channel is commwright's, and the same content pasted into a release body is a message again. The close now holds on the surface that loads first as well as at the description level, where trigger rows #35-#37 measure it.

**foundation capstone:** Forge Run — one orchestration prompt driving all ten members end-to-end (stored 2026-07-12 · roster updated to eight 2026-07-13 · roster reconfirmed 2026-07-14, pack self-audit, no change · re-run after any member's major version bump). Live runs: brandwright build, 2026-07-13 · **Forge Run 3, 2026-07-23** — the 1.1.0 rebuild as build proof, all eight doctrines exercised in anger (skillwright build/integrate conventions, evalwright diff-scoped suite refresh, tokenwright measurement + C-1 audit, lorewright-grade source verification on four baselines, brandwright centralization, commwright-shaped release comms, agentwright platform research, promptwright model snapshot + Entry — Model); roster reconfirmed at 1.1.x, no change. **Roster 8 → 9 on 2026-07-30** with the addition of `revenantworks-foundation-rigwright`; capstone roster line updated, capstone run not re-triggered (a member add updates the line only). **Roster 9 → 10 on 2026-08-18** with the addition of `revenantworks-foundation-dispatchwright`; capstone roster line updated, capstone run not re-triggered (a member add updates the line only); the routing-seam table was not extended for the new member this pass — recorded as owed in the member's own README rather than left silent.

**foundation canonical repo:** `github.com/revenantworks/claude-skills` — source of truth for member drift audits (registered 2026-07-13; repository renamed from `citadel` 2026-08-17, GitHub redirects the old name). Relocated from the personal account 2026-07-14; the prior copy is a private archive. Nothing else carries the URL: manifests are generated from here, and every other file says "the registered canonical repo."

**localops members** *(canonical roster — pack manifests are generated from this table)*

| Member | Job | Route there when |
|---|---|---|
| `revenantworks-localops-lmstudiorunner` | Hands work to a local LM Studio model and verifies what comes back — work-class scoring, live model audit, two verification modes | The deliverable is work delegated to a local or offline model, a model-fit or switch answer, or an unattended local run that keeps only checked results |

**localops budgets** *(body-footprint table)*

| Member | Budget (tokens) | Why |
|---|---|---|
| `revenantworks-localops-lmstudiorunner` | 3000 | measured ≈2140 at 1.0.0 build; the two-mode rule and its reason are body-resident because a run must decide the mode before it opens anything, and the discovery and diagnosis steps state their own thresholds; references hold the class tables, the API fields and the card shape |

**localops seams:** none — a one-member pack has no boundary pair. The three cross-pack handoffs (promptwright for cloud tiers and prompt text, agentwright for the machinery around a scheduled run) are carried by lmstudiorunner's own description; a seam row is owed when a second localops member lands.

**localops capstone:** none — a one-member pack (2026-09-09); revisit when the roster reaches three.

**localops canonical repo:** `github.com/revenantworks/claude-skills` — source of truth for member drift audits (registered 2026-09-09).

**gamedev members** *(canonical roster — pack manifests are generated from this table)*

| Member | Job | Route there when |
|---|---|---|
| `revenantworks-gamedev-pixelsmith` | Directs pixel art that must read at several zoom scales — per-band rules, terrain-versus-unit contrast, a one-scene look test, artist and generator briefs | The deliverable is a multi-scale pixel-art rule set, a look-test scorecard, an artist or generator brief, or an audit of existing art against those rules |
| `revenantworks-gamedev-godotsmith` | Godot 4.x project conventions and the proof that a build is actually green — what a run's numbers may mean, the CI guards that keep them honest, who may close a gate, and the structural rules that make a project provable | The deliverable is an audit of whether a test run can be believed, a CI assertion set, a milestone gate ruling, or a read of Godot code against scene, signal, lifetime, determinism or repository conventions |

**gamedev budgets** *(body-footprint table — one row per member; `build.py` validates each member's measured SKILL.md body against its row)*

| Member | Budget (tokens) | Why |
|---|---|---|
| `revenantworks-gamedev-godotsmith` | 2600 | measured ≈2145 at the 1.0.0 build; the ten laws are body-resident because a run must not open a file to know what it is enforcing, and the four entry points each state the one or two rules that decide the run before any reference loads; all eight doctrine files hold procedure, worked commands and the borrowed-practice detail |
| `revenantworks-gamedev-pixelsmith` | 3500 | measured ≈2360 at 1.0.0 build; the band model and the five laws are body-resident because a run must not open a file to know what it is enforcing; the references hold numbers, procedure, and templates |

**gamedev seams** *(routing-seam table — one row per boundary pair; first declared 2026-09-12, when godotsmith made the pack two members)*

| Seam | Left owns | Right owns | Router keys on | Cold-listing signal |
|---|---|---|---|---|
| pixelsmith ↔ godotsmith | Whether the art **reads** — palette and silhouette per zoom band, terrain-versus-unit contrast, the one-scene look test, and briefs for whoever draws or generates it | Whether the build is **provable** — what a test run's numbers may mean, the CI assertions that keep them honest, who may close a milestone gate, and the scene, signal, lifetime and determinism conventions underneath | What the answer would be measured against. A judgement by eye — does this read, does it hold at distance — is pixelsmith. A judgement against a count, a threshold or a stated bar is godotsmith. Recorded edge: "the units vanish when zoomed out" is pixelsmith even though a band alpha is a number, because the complaint is about perception; the same symptom traced to a clamp nobody checked against the band spacing is godotsmith, because the bar is arithmetic | both descriptions |

The brandwright handoff (a brand palette is an input, never defined here) crosses packs and is carried by pixelsmith's own description.

**gamedev capstone:** none — a two-member pack (godotsmith added 2026-09-12); revisit when the roster reaches three.

**gamedev canonical repo:** `github.com/revenantworks/claude-skills` — source of truth for member drift audits (registered 2026-08-29). Nothing else carries the URL: manifests are generated from here, and every other file says "the registered canonical repo."
